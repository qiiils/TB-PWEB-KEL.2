<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Praktikum Data Mining</title>
    <link href="../output.css" rel="stylesheet">
</head>

<body class="bg-gray-900 text-white">
  <nav class="bg-black p-10 flex justify-between items-center h-20">
    <div class="space-x-4">
      <a href="/home" class="text-white hover:bg-blue-500 hover:text-white transition-colors duration-300 px-4 py-2 rounded-md border-b-2 border-transparent">Home</a>
      <a href="/notifikasi" class="text-white hover:bg-blue-500 hover:text-white transition-colors duration-300 px-4 py-2 rounded-md border-b-2 border-transparent">Notifikasi</a>
      <a href="/profile" class="text-white hover:bg-blue-500 hover:text-white transition-colors duration-300 px-4 py-2 rounded-md border-b-2 border-transparent">Profile</a>
      <a href="/logout" class="text-white hover:bg-blue-500 hover:text-white transition-colors duration-300 px-4 py-2 rounded-md border-b-2 border-transparent">Logout</a>
    </div>
    <div class="space-x-4 flex items-center">
      <% if (userRole === 'aslab') { %>
        <button id="buatKelasBtn" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">Buat Kelas</button>
      <% } %>
      <% if (userRole === 'mahasiswa') { %>
        <a href="/enrollment" class="bg-green-500 hover:bg-green-700 text-white font-bold py-2 px-4 rounded">Enrollment</a>
      <% } %>
      <div class="flex items-center space-x-2">
        <div class="bg-gray-700 rounded-full w-10 h-10 flex items-center justify-center">
          <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5.121 17.804A12.074 12.074 0 0012 21a12.074 12.074 0 006.879-3.196M15 10a3 3 0 11-6 0 3 3 0 016 0z"></path>
          </svg>
        </div>
        <span class="text-white font-medium"><%= userName %></span>
      </div>
    </div>
  </nav>

  <div class="min-h-screen flex items-center justify-center w-full px-4 mt-4">
    <div class="p-6 rounded-lg shadow-lg w-1/2 border-4 border-blue-500">
      <div class="text-start mb-6">
        <h2 class="text-2xl font-bold text-blue-500">Profile</h2>
      </div>
      <div class="mb-4">
        <label class="block text-sm font-medium text-blue-500">NIM</label>
        <input type="text" class="mt-1 p-2 border border-blue-500 bg-gray-800 rounded-lg w-1/2" value="<%= user.nim %>" readonly>
      </div>
      <div class="mb-4">
        <label class="block text-sm font-medium text-blue-500">Name</label>
        <input type="text" class="mt-1 p-2 border border-blue-500 bg-gray-800 rounded-lg w-1/2" value="<%= user.name %>" readonly>
      </div>
      <div class="mb-4">
        <label class="block text-sm font-medium text-blue-500">Email</label>
        <input type="email" class="mt-1 p-2 border border-blue-500 bg-gray-800 rounded-lg w-1/2" value="<%= user.email %>" readonly>
      </div>
      <div class="text-start mb-6">
        <h3 class="text-lg font-bold text-blue-500">Change Password</h3>
      </div>
      <form action="/change-password" method="POST">
        <div class="mb-4">
          <label class="block text-sm font-medium text-blue-500">Current Password</label>
          <input type="password" name="current_password" class="mt-1 p-2 border border-blue-500 bg-gray-800 rounded-lg w-1/2" required>
        </div>
        <div class="mb-4">
          <label class="block text-sm font-medium text-blue-500">New Password</label>
          <input type="password" name="new_password" class="mt-1 p-2 border border-blue-500 bg-gray-800 rounded-lg w-1/2" required>
        </div>
        <div class="mb-4">
          <label class="block text-sm font-medium text-blue-500">Confirm New Password</label>
          <input type="password" name="confirm_new_password" class="mt-1 p-2 border border-blue-500 bg-gray-800 rounded-lg w-1/2" required>
        </div>
        <div class="flex items-start justify-start">
          <button type="submit" class="bg-blue-500 text-white px-4 py-2 rounded-lg hover:bg-blue-600">Change Password</button>
        </div>
      </form>
    </div>
  </div>
</body>

</html>
